# 订阅速率限制 - 开发实施计划

## 文件结构

### 新建文件
- `service/rate_limit.go` — 限速服务：窗口 key 计算、Redis Sorted Set 操作、检查/记录/清理
- `service/rate_limit_test.go` — 限速服务单元测试

### 修改文件
- `model/subscription.go` — SubscriptionPlan 新增 PlanType/RateLimitTokensPerWindow/RateLimitWeeklyMultiplier；UserSubscription 同样新增三个快照字段；新增 PlanType 常量；CreateUserSubscriptionFromPlanTx 快照限速参数；ExpireDueSubscriptions 和 AdminInvalidateUserSubscription 触发清理
- `controller/subscription.go` — AdminCreateSubscriptionPlan/AdminUpdateSubscriptionPlan 新增限速参数校验；新增 GetSubscriptionRateLimits
- `controller/relay.go` — PreConsumeBilling 之前插入 TPM 限速检查；relay 请求完成后添加限速用量记录和响应头注入（覆盖所有 relay 模式）
- `service/billing_session.go` — SubscriptionFunding 增加 PlanType 字段，用于响应头注入判断
- `relay/common/relay_info.go` — RelayInfo 增加限速相关字段
- `router/api-router.go` — 注册 GET /api/subscription/rate-limits
- `i18n/keys.go` + `i18n/locales/en/` + `i18n/locales/zh/` — 限速相关 i18n key
- `web/default/src/features/subscriptions/types.ts` — 新增 plan_type、rate_limit_tokens_per_window、rate_limit_weekly_multiplier 字段
- `web/default/src/features/subscriptions/lib/plan-form.ts` — 表单 schema 和转换函数新增限速字段
- `web/default/src/features/subscriptions/api.ts` — 新增 getRateLimits API 函数
- `web/default/src/features/subscriptions/components/subscriptions-mutate-drawer.tsx` — 新增限速配置区域
- `web/default/src/features/wallet/components/subscription-plans-card.tsx` — 新增限速状态展示和套餐卡片限速参数
- `web/default/src/features/subscriptions/components/dialogs/subscription-purchase-dialog.tsx` — 新增购买弹窗限速参数展示
- `web/default/src/i18n/locales/en.json` — 新增限速相关英文翻译
- `web/default/src/i18n/locales/zh.json` — 新增限速相关中文翻译

---

## T1: 数据库迁移 — 新增限速字段

**类型：** 数据库变更（DDL）
**测试文件：** 无（GORM AutoMigrate）

### 步骤1：在 SubscriptionPlan 结构体新增字段

> 以下字段定义引用 `04_model_interface.md` 第 1、2 节（subscription_plans 新增字段、user_subscriptions 新增字段）和 Redis 数据设计。

打开 `model/subscription.go`，在 SubscriptionPlan 结构体的 `QuotaResetCustomSeconds` 字段之后、`CreatedAt` 字段之前添加三个新字段：

```go
// Plan type: "api" (API quota-based) or "coding_plan" (token rate-limited)
PlanType string `json:"plan_type" gorm:"type:varchar(32);not null;default:'api'"`

// 5-hour window token limit (only effective for coding_plan, 0 for api)
RateLimitTokensPerWindow int `json:"rate_limit_tokens_per_window" gorm:"type:int;not null;default:0"`

// Weekly multiplier: weekly_limit = tokens_per_window × multiplier (only effective for coding_plan, 0 for api)
RateLimitWeeklyMultiplier int `json:"rate_limit_weekly_multiplier" gorm:"type:int;not null;default:0"`
```

同时在 UserSubscription 结构体的 `PrevUserGroup` 字段之后、`CreatedAt` 字段之前添加同样的三个快照字段：

```go
// Snapshot from SubscriptionPlan at purchase time
PlanType string `json:"plan_type" gorm:"type:varchar(32);not null;default:'api'"`
RateLimitTokensPerWindow int `json:"rate_limit_tokens_per_window" gorm:"type:int;not null;default:0"`
RateLimitWeeklyMultiplier int `json:"rate_limit_weekly_multiplier" gorm:"type:int;not null;default:0"`
```

在文件顶部常量区域（约行 17-33 附近）添加 PlanType 常量：

```go
const (
	PlanTypeAPI        = "api"
	PlanTypeCodingPlan = "coding_plan"
)
```

### 步骤2：验证迁移

编译并启动应用，确认 AutoMigrate 自动添加了 6 个新列（每个表 3 个）：

```bash
go build -o new-api && ./new-api
```

在 SQLite 环境下验证：

```bash
sqlite3 *.db "PRAGMA table_info(subscription_plans);" | grep -E "plan_type|rate_limit"
sqlite3 *.db "PRAGMA table_info(user_subscriptions);" | grep -E "plan_type|rate_limit"
```

预期输出：每个表各出现 3 行新列记录，类型和默认值与 GORM tag 一致。

### 步骤3：提交

```bash
git add model/subscription.go
git commit -m "feat(subscription): add rate limit fields to SubscriptionPlan and UserSubscription models"
```

---

## T2: 限速服务 — 核心工具函数

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

### 步骤1：编写失败测试

创建 `service/rate_limit.go` 和 `service/rate_limit_test.go`。

先写测试文件 `service/rate_limit_test.go`：

```go
package service

import (
	"testing"
	"time"
)

func TestCalc5hWindowKey(t *testing.T) {
	tests := []struct {
		name     string
		utcHour  int
		expected int
	}{
		{"0:00-4:59 -> window 0", 0, 0},
		{"5:00-9:59 -> window 1", 5, 1},
		{"10:00-14:59 -> window 2", 10, 2},
		{"15:00-19:59 -> window 3", 15, 3},
		{"20:00-23:59 -> window 4", 20, 4},
		{"4:59 -> window 0", 4, 0},
		{"23:00 -> window 4", 23, 4},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := Calc5hWindowKey(tt.utcHour)
			if got != tt.expected {
				t.Errorf("Calc5hWindowKey(%d) = %d, want %d", tt.utcHour, got, tt.expected)
			}
		})
	}
}

func TestCalc5hWindowResetAt(t *testing.T) {
	// UTC 2026-05-18 03:30:00 is in window 0 (0:00-4:59), reset at 05:00
	ts := time.Date(2026, 5, 18, 3, 30, 0, 0, time.UTC)
	resetAt := Calc5hWindowResetAt(ts)
	expected := time.Date(2026, 5, 18, 5, 0, 0, 0, time.UTC).Unix()
	if resetAt != expected {
		t.Errorf("Calc5hWindowResetAt(03:30) = %d, want %d", resetAt, expected)
	}

	// UTC 2026-05-18 21:00:00 is in window 4 (20:00-23:59), reset at next day 00:00
	ts2 := time.Date(2026, 5, 18, 21, 0, 0, 0, time.UTC)
	resetAt2 := Calc5hWindowResetAt(ts2)
	expected2 := time.Date(2026, 5, 19, 0, 0, 0, 0, time.UTC).Unix()
	if resetAt2 != expected2 {
		t.Errorf("Calc5hWindowResetAt(21:00) = %d, want %d", resetAt2, expected2)
	}
}

func TestCalcWeekWindowKey(t *testing.T) {
	// 2026-05-18 is a Monday, ISO week 20
	ts := time.Date(2026, 5, 18, 12, 0, 0, 0, time.UTC)
	got := CalcWeekWindowKey(ts)
	if got != "2026-W20" {
		t.Errorf("CalcWeekWindowKey(Monday) = %s, want 2026-W20", got)
	}

	// 2026-05-17 is a Sunday, same ISO week 20
	ts2 := time.Date(2026, 5, 17, 23, 59, 59, 0, time.UTC)
	got2 := CalcWeekWindowKey(ts2)
	if got2 != "2026-W20" {
		t.Errorf("CalcWeekWindowKey(Sunday) = %s, want 2026-W20", got2)
	}
}

func TestCalcWeekResetAt(t *testing.T) {
	// Any time during week 20 resets at Monday 00:00 UTC of week 21
	ts := time.Date(2026, 5, 18, 12, 0, 0, 0, time.UTC)
	resetAt := CalcWeekResetAt(ts)
	expected := time.Date(2026, 5, 25, 0, 0, 0, 0, time.UTC).Unix()
	if resetAt != expected {
		t.Errorf("CalcWeekResetAt() = %d, want %d", resetAt, expected)
	}
}

func TestRateLimitRedisKeys(t *testing.T) {
	subId := 123
	windowKey := 2
	weekKey := "2026-W20"

	k5h := RateLimitRedisKey5h(subId, windowKey)
	kWeek := RateLimitRedisKeyWeek(subId, weekKey)

	if k5h != "rate_limit:123:5h:2" {
		t.Errorf("5h key = %s, want rate_limit:123:5h:2", k5h)
	}
	if kWeek != "rate_limit:123:week:2026-W20" {
		t.Errorf("week key = %s, want rate_limit:123:week:2026-W20", kWeek)
	}
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestCalc5h|TestRateLimitRedis" -v
```

预期：编译失败（函数未定义）。

### 步骤3：编写最少实现

创建 `service/rate_limit.go`：

```go
package service

import (
	"fmt"
	"time"
)

// Calc5hWindowKey returns the 5-hour window index (0-4) for a given UTC hour.
func Calc5hWindowKey(utcHour int) int {
	return utcHour / 5
}

// Calc5hWindowResetAt returns the Unix timestamp when the current 5h window resets.
func Calc5hWindowResetAt(now time.Time) int64 {
	windowIdx := Calc5hWindowKey(now.Hour())
	resetHour := (windowIdx + 1) * 5
	if resetHour >= 24 {
		return time.Date(now.Year(), now.Month(), now.Day()+1, 0, 0, 0, 0, time.UTC).Unix()
	}
	return time.Date(now.Year(), now.Month(), now.Day(), resetHour, 0, 0, 0, time.UTC).Unix()
}

// CalcWeekWindowKey returns the ISO week key (e.g. "2026-W20") for a given time.
func CalcWeekWindowKey(now time.Time) string {
	year, week := now.ISOWeek()
	return fmt.Sprintf("%d-W%02d", year, week)
}

// CalcWeekResetAt returns the Unix timestamp when the current week resets (next Monday 00:00 UTC).
func CalcWeekResetAt(now time.Time) int64 {
	weekday := int(now.Weekday())
	if weekday == 0 {
		weekday = 7
	}
	nextMonday := now.AddDate(0, 0, 8-weekday)
	return time.Date(nextMonday.Year(), nextMonday.Month(), nextMonday.Day(), 0, 0, 0, 0, time.UTC).Unix()
}

// RateLimitRedisKey5h builds the Redis key for a 5h window.
func RateLimitRedisKey5h(subscriptionId int, windowKey int) string {
	return fmt.Sprintf("rate_limit:%d:5h:%d", subscriptionId, windowKey)
}

// RateLimitRedisKeyWeek builds the Redis key for a week window.
func RateLimitRedisKeyWeek(subscriptionId int, weekKey string) string {
	return fmt.Sprintf("rate_limit:%d:week:%s", subscriptionId, weekKey)
}
```

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestCalc5h|TestRateLimitRedis" -v
```

预期：所有测试通过。

### 步骤5：提交

```bash
git add service/rate_limit.go service/rate_limit_test.go
git commit -m "feat(rate-limit): add window key calculation and Redis key formatting utilities"
```

---

## T3: 限速服务 — 业务操作（检查、记录、清理）

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

### 步骤1：编写失败测试

在 `service/rate_limit_test.go` 中追加测试：

```go
func TestCheckRateLimit_NotExceeded(t *testing.T) {
	// [示意] 需要真实 Redis 连接或 miniredis mock
	// 使用 miniredis 模拟
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatal(err)
	}
	defer mr.Close()

	// 配置 common 使用 miniredis
	origClient := common.RDB
	defer func() { common.RDB = origClient }()

	mr.ZAdd("rate_limit:1:5h:2", miniredis.Z{Score: 100, Member: "req_1:100"})
	mr.ZAdd("rate_limit:1:week:2026-W20", miniredis.Z{Score: 100, Member: "req_1:100"})

	status, exceeded := CheckSubscriptionRateLimit(1, 500000, 20)
	if exceeded {
		t.Error("should not be exceeded with low usage")
	}
	if status.Window5h.Used != 100 {
		t.Errorf("5h used = %d, want 100", status.Window5h.Used)
	}
}

func TestRecordRateLimitUsage(t *testing.T) {
	// [示意] 需要真实 Redis 连接或 miniredis mock
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatal(err)
	}
	defer mr.Close()

	err = RecordRateLimitUsage(1, "req_test", 5000)
	if err != nil {
		t.Errorf("RecordRateLimitUsage failed: %v", err)
	}

	// Verify ZADD was called (key exists)
	exists := mr.Exists("rate_limit:1:5h:")
	if !exists {
		t.Error("expected 5h key to exist after recording")
	}
}

func TestCleanupRateLimitData(t *testing.T) {
	// [示意] 需要真实 Redis 连接或 miniredis mock
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatal(err)
	}
	defer mr.Close()

	// Pre-populate some keys
	mr.Set("rate_limit:1:5h:2", "dummy")
	mr.Set("rate_limit:1:week:2026-W20", "dummy")

	err = CleanupRateLimitData(1)
	if err != nil {
		t.Errorf("CleanupRateLimitData failed: %v", err)
	}
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestCheckRateLimit|TestRecordRateLimit|TestCleanupRateLimit" -v
```

预期：编译失败（函数未定义）。

### 步骤3：编写最少实现

在 `service/rate_limit.go` 中追加以下类型和函数。注意 Redis 操作使用 `common.RDB`（项目已有的 go-redis 客户端）：

```go
import (
	"context"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/QuantumNous/new-api/common"
	"github.com/QuantumNous/new-api/logger"
)

// WindowStatus represents the rate limit status for a single window.
type WindowStatus struct {
	Limit     int   `json:"limit"`
	Used      int   `json:"used"`
	Remaining int   `json:"remaining"`
	ResetAt   int64 `json:"reset_at"`
}

// RateLimitStatus represents the full rate limit status for a subscription.
type RateLimitStatus struct {
	SubscriptionId int          `json:"subscription_id"`
	PlanTitle      string       `json:"plan_title"`
	PlanType       string       `json:"plan_type"`
	Window5h       WindowStatus `json:"window_5h"`
	WindowWeek     WindowStatus `json:"window_week"`
}

// CheckSubscriptionRateLimit checks if the subscription has exceeded its rate limits.
// Returns the current status and whether the rate limit is exceeded.
func CheckSubscriptionRateLimit(subscriptionId int, limit5h int, weeklyMultiplier int) (*RateLimitStatus, bool) {
	now := time.Now().UTC()
	status := &RateLimitStatus{
		SubscriptionId: subscriptionId,
	}

	if limit5h <= 0 || weeklyMultiplier <= 0 {
		return status, false
	}

	limitWeek := limit5h * weeklyMultiplier

	// Check 5h window
	windowKey5h := Calc5hWindowKey(now.Hour())
	redisKey5h := RateLimitRedisKey5h(subscriptionId, windowKey5h)
	used5h := queryWindowUsage(redisKey5h)
	remaining5h := limit5h - used5h
	if remaining5h < 0 {
		remaining5h = 0
	}
	status.Window5h = WindowStatus{
		Limit:     limit5h,
		Used:      used5h,
		Remaining: remaining5h,
		ResetAt:   Calc5hWindowResetAt(now),
	}

	// Check week window
	weekKey := CalcWeekWindowKey(now)
	redisKeyWeek := RateLimitRedisKeyWeek(subscriptionId, weekKey)
	usedWeek := queryWindowUsage(redisKeyWeek)
	remainingWeek := limitWeek - usedWeek
	if remainingWeek < 0 {
		remainingWeek = 0
	}
	status.WindowWeek = WindowStatus{
		Limit:     limitWeek,
		Used:      usedWeek,
		Remaining: remainingWeek,
		ResetAt:   CalcWeekResetAt(now),
	}

	exceeded := used5h >= limit5h || usedWeek >= limitWeek
	return status, exceeded
}

// queryWindowUsage sums all token counts in a Redis Sorted Set for the given key.
func queryWindowUsage(redisKey string) int {
	if common.RDB == nil {
		return 0
	}
	ctx := context.Background()
	members, err := common.RDB.ZRangeByScore(ctx, redisKey, &redis.ZRangeBy{
		Min: "-inf",
		Max: "+inf",
	}).Result()
	if err != nil {
		if !errors.Is(err, redis.Nil) {
			logger.LogError(nil, fmt.Sprintf("rate limit query failed for key %s: %v", redisKey, err))
		}
		return 0
	}
	total := 0
	for _, m := range members {
		parts := strings.SplitN(m, ":", 2)
		if len(parts) == 2 {
			if count, err := strconv.Atoi(parts[1]); err == nil {
				total += count
			}
		}
	}
	return total
}

// RecordRateLimitUsage records a token consumption entry into the rate limit counters.
func RecordRateLimitUsage(subscriptionId int, requestId string, tokenCount int) error {
	if common.RDB == nil {
		return nil
	}
	if subscriptionId <= 0 || tokenCount <= 0 {
		return nil
	}

	now := time.Now().UTC()
	timestamp := now.Unix()
	member := fmt.Sprintf("%s:%d", requestId, tokenCount)

	ctx := context.Background()
	pipe := common.RDB.Pipeline()

	// 5h window
	windowKey5h := Calc5hWindowKey(now.Hour())
	redisKey5h := RateLimitRedisKey5h(subscriptionId, windowKey5h)
	pipe.ZAdd(ctx, redisKey5h, redis.Z{Score: float64(timestamp), Member: member})
	pipe.Expire(ctx, redisKey5h, 6*time.Hour)

	// Week window
	weekKey := CalcWeekWindowKey(now)
	redisKeyWeek := RateLimitRedisKeyWeek(subscriptionId, weekKey)
	pipe.ZAdd(ctx, redisKeyWeek, redis.Z{Score: float64(timestamp), Member: member})
	pipe.Expire(ctx, redisKeyWeek, 8*24*time.Hour)

	_, err := pipe.Exec(ctx)
	if err != nil {
		logger.LogError(nil, fmt.Sprintf("failed to record rate limit usage for subscription %d: %v", subscriptionId, err))
		return err
	}
	return nil
}

// CleanupRateLimitData removes all rate limit Redis keys for a subscription.
func CleanupRateLimitData(subscriptionId int) error {
	if common.RDB == nil {
		return nil
	}
	ctx := context.Background()
	pattern := fmt.Sprintf("rate_limit:%d:*", subscriptionId)

	var cursor uint64
	var totalDeleted int
	for {
		keys, nextCursor, err := common.RDB.Scan(ctx, cursor, pattern, 100).Result()
		if err != nil {
			logger.LogError(nil, fmt.Sprintf("rate limit key scan failed for subscription %d: %v", subscriptionId, err))
			return err
		}
		if len(keys) > 0 {
			deleted, err := common.RDB.Del(ctx, keys...).Result()
			if err != nil {
				logger.LogError(nil, fmt.Sprintf("rate limit key delete failed for subscription %d: %v", subscriptionId, err))
				return err
			}
			totalDeleted += int(deleted)
		}
		cursor = nextCursor
		if cursor == 0 {
			break
		}
	}
	return nil
}
```

注意：需要在文件顶部 import 中添加 `"errors"` 和 `"github.com/redis/go-redis/v9"`。

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestCheckRateLimit|TestRecordRateLimit|TestCleanupRateLimit" -v
```

预期：所有测试通过。

### 步骤5：提交

```bash
git add service/rate_limit.go service/rate_limit_test.go
git commit -m "feat(rate-limit): add check, record, and cleanup business operations"
```

---

## T4: 限速状态查询 API

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

### 步骤1：编写失败测试

在 `service/rate_limit_test.go` 中追加测试：

```go
func TestGetUserRateLimitStatus_NoCodingPlan(t *testing.T) {
	// User with no CodingPlan subscriptions should return empty list
	statuses, err := GetUserRateLimitStatus(99999)
	if err != nil {
		t.Errorf("unexpected error: %v", err)
	}
	if len(statuses) != 0 {
		t.Errorf("expected empty list, got %d items", len(statuses))
	}
}

func TestGetUserRateLimitStatus_WithCodingPlan(t *testing.T) {
	// [示意] 需要数据库和 Redis 环境
	// 创建 CodingPlan 类型的套餐和订阅后查询
	statuses, err := GetUserRateLimitStatus(1)
	if err != nil {
		t.Errorf("unexpected error: %v", err)
	}
	for _, s := range statuses {
		if s.PlanType != "coding_plan" {
			t.Errorf("expected plan_type coding_plan, got %s", s.PlanType)
		}
	}
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestGetUserRateLimitStatus" -v
```

### 步骤3：编写最少实现

在 `service/rate_limit.go` 中追加：

```go
// GetUserRateLimitStatus returns rate limit status for all active CodingPlan subscriptions of a user.
func GetUserRateLimitStatus(userId int) ([]RateLimitStatus, error) {
	if userId <= 0 {
		return nil, nil
	}
	subs, err := model.GetActiveCodingPlanSubscriptions(userId)
	if err != nil {
		return nil, err
	}
	if len(subs) == 0 {
		return []RateLimitStatus{}, nil
	}

	result := make([]RateLimitStatus, 0, len(subs))
	for _, sub := range subs {
		status, _ := CheckSubscriptionRateLimit(sub.Id, sub.RateLimitTokensPerWindow, sub.RateLimitWeeklyMultiplier)
		plan, _ := model.GetSubscriptionPlanById(sub.PlanId)
		planTitle := ""
		if plan != nil {
			planTitle = plan.Title
		}
		status.PlanTitle = planTitle
		status.PlanType = sub.PlanType
		result = append(result, *status)
	}
	return result, nil
}
```

在 `model/subscription.go` 中新增查询函数：

```go
// GetActiveCodingPlanSubscriptions returns all active CodingPlan subscriptions for a user.
func GetActiveCodingPlanSubscriptions(userId int) ([]UserSubscription, error) {
	if userId <= 0 {
		return nil, nil
	}
	now := GetDBTimestamp()
	var subs []UserSubscription
	err := DB.Where("user_id = ? AND status = ? AND end_time > ? AND plan_type = ?",
		userId, "active", now, PlanTypeCodingPlan).
		Order("end_time desc, id desc").
		Find(&subs).Error
	if err != nil {
		return nil, err
	}
	return subs, nil
}
```

在 `controller/subscription.go` 中新增控制器函数：

```go
func GetSubscriptionRateLimits(c *gin.Context) {
	userId := c.GetInt("id")
	statuses, err := service.GetUserRateLimitStatus(userId)
	if err != nil {
		common.ApiSuccess(c, []interface{}{})
		return
	}
	if statuses == nil {
		statuses = []service.RateLimitStatus{}
	}
	common.ApiSuccess(c, statuses)
}
```

在 `router/api-router.go` 的 subscriptionRoute 组中注册路由（在 `subscriptionRoute.GET("/self", ...)` 之后添加）：

```go
subscriptionRoute.GET("/rate-limits", controller.GetSubscriptionRateLimits)
```

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestGetUserRateLimitStatus" -v
go build -o new-api
```

预期：测试通过，编译成功。

### 步骤5：提交

```bash
git add service/rate_limit.go service/rate_limit_test.go model/subscription.go controller/subscription.go router/api-router.go
git commit -m "feat(rate-limit): add rate limit status query API (GET /api/subscription/rate-limits)"
```

---

## T5: 套餐 CRUD 校验变更

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

### 步骤1：编写失败测试

在 `service/rate_limit_test.go` 中追加测试：

```go
func TestValidateRateLimitParams_APIPlan(t *testing.T) {
	err := ValidateRateLimitParams("api", 0, 0)
	if err != nil {
		t.Errorf("API plan with zero rate limit fields should pass: %v", err)
	}
}

func TestValidateRateLimitParams_CodingPlanValid(t *testing.T) {
	err := ValidateRateLimitParams("coding_plan", 500000, 20)
	if err != nil {
		t.Errorf("valid CodingPlan should pass: %v", err)
	}
}

func TestValidateRateLimitParams_CodingPlanMissingTokens(t *testing.T) {
	err := ValidateRateLimitParams("coding_plan", 0, 20)
	if err == nil {
		t.Error("CodingPlan with zero tokens_per_window should fail")
	}
}

func TestValidateRateLimitParams_CodingPlanMissingMultiplier(t *testing.T) {
	err := ValidateRateLimitParams("coding_plan", 500000, 0)
	if err == nil {
		t.Error("CodingPlan with zero weekly_multiplier should fail")
	}
}

func TestValidateRateLimitParams_CodingPlanNegativeTokens(t *testing.T) {
	err := ValidateRateLimitParams("coding_plan", -1, 20)
	if err == nil {
		t.Error("CodingPlan with negative tokens_per_window should fail")
	}
}

func TestValidateRateLimitParams_InvalidPlanType(t *testing.T) {
	err := ValidateRateLimitParams("invalid", 0, 0)
	if err == nil {
		t.Error("invalid plan_type should fail")
	}
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestValidateRateLimitParams" -v
```

### 步骤3：编写最少实现

在 `service/rate_limit.go` 中追加：

```go
// ValidateRateLimitParams validates rate limit parameters for a subscription plan.
func ValidateRateLimitParams(planType string, tokensPerWindow int, weeklyMultiplier int) error {
	switch planType {
	case PlanTypeAPI:
		return nil
	case PlanTypeCodingPlan:
		if tokensPerWindow < 1 {
			return fmt.Errorf("rate_limit_tokens_per_window is required for coding_plan and must be >= 1")
		}
		if weeklyMultiplier < 1 {
			return fmt.Errorf("rate_limit_weekly_multiplier is required for coding_plan and must be >= 1")
		}
		return nil
	default:
		return fmt.Errorf("invalid plan_type: %s", planType)
	}
}
```

注意：这里引用的 `PlanTypeAPI` 和 `PlanTypeCodingPlan` 常量定义在 `model/subscription.go` 中。有两种处理方式：在本文件中重复定义常量（不推荐），或在 service 包中导入 model 包的常量（推荐）。鉴于 service 包已经导入了 model 包，直接使用 `model.PlanTypeAPI` 和 `model.PlanTypeCodingPlan`：

```go
import "github.com/QuantumNous/new-api/model"

func ValidateRateLimitParams(planType string, tokensPerWindow int, weeklyMultiplier int) error {
	switch planType {
	case model.PlanTypeAPI:
		return nil
	case model.PlanTypeCodingPlan:
		if tokensPerWindow < 1 {
			return fmt.Errorf("rate_limit_tokens_per_window is required for coding_plan and must be >= 1")
		}
		if weeklyMultiplier < 1 {
			return fmt.Errorf("rate_limit_weekly_multiplier is required for coding_plan and must be >= 1")
		}
		return nil
	default:
		return fmt.Errorf("invalid plan_type: %s", planType)
	}
}
```

在 `controller/subscription.go` 的 `AdminCreateSubscriptionPlan` 函数中，在 `req.Plan.QuotaResetPeriod = model.NormalizeResetPeriod(...)` 之后（约行 164 之后），添加限速参数校验：

```go
	// Validate rate limit params
	if req.Plan.PlanType == "" {
		req.Plan.PlanType = model.PlanTypeAPI
	}
	if err := service.ValidateRateLimitParams(req.Plan.PlanType, req.Plan.RateLimitTokensPerWindow, req.Plan.RateLimitWeeklyMultiplier); err != nil {
		common.ApiErrorMsg(c, err.Error())
		return
	}
	if req.Plan.PlanType == model.PlanTypeAPI {
		req.Plan.RateLimitTokensPerWindow = 0
		req.Plan.RateLimitWeeklyMultiplier = 0
	}
```

在 `AdminUpdateSubscriptionPlan` 函数中做相同的校验（在行 231 `req.Plan.QuotaResetPeriod = ...` 之后添加相同代码块）。

在 `i18n/keys.go` 中新增限速相关 i18n key：

```go
ErrSubRateLimitTokensRequired  // "rate_limit_tokens_per_window is required for coding_plan and must be >= 1"
ErrSubRateLimitMultiplierRequired  // "rate_limit_weekly_multiplier is required for coding_plan and must be >= 1"
ErrSubRateLimitInvalidPlanType  // "invalid plan_type"
```

在 `i18n/locales/en/` 和 `i18n/locales/zh/` 中添加对应的英文和中文翻译。

然后将 `ValidateRateLimitParams` 中的 `fmt.Errorf` 错误消息改为返回 i18n key（或错误码），在 controller 层使用 `common.ApiErrorI18n(c, key, args...)` 返回。T7 step 3b 的 429 响应体中的 `message` 字段同理，使用 i18n 获取用户语言的提示文案。

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestValidateRateLimitParams" -v
go build -o new-api
```

### 步骤5：提交

```bash
git add service/rate_limit.go service/rate_limit_test.go controller/subscription.go i18n/keys.go i18n/locales/
git commit -m "feat(rate-limit): add rate limit parameter validation for plan CRUD with i18n"
```

---

## T6: 订阅快照 + 过期清理钩子

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

### 步骤1：编写失败测试

在 `service/rate_limit_test.go` 中追加测试：

```go
func TestCleanupOnSubscriptionExpiry(t *testing.T) {
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatal(err)
	}
	defer mr.Close()

	// Pre-populate rate limit keys for subscription 1
	mr.ZAdd("rate_limit:1:5h:2", miniredis.Z{Score: 100, Member: "req_1:100"})
	mr.ZAdd("rate_limit:1:week:2026-W20", miniredis.Z{Score: 200, Member: "req_2:200"})

	origClient := common.RDB
	defer func() { common.RDB = origClient }()

	err = CleanupRateLimitData(1)
	if err != nil {
		t.Errorf("CleanupRateLimitData failed: %v", err)
	}

	// Verify keys are removed
	if mr.Exists("rate_limit:1:5h:2") {
		t.Error("expected 5h key to be deleted after cleanup")
	}
	if mr.Exists("rate_limit:1:week:2026-W20") {
		t.Error("expected week key to be deleted after cleanup")
	}
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestCleanupOnSubscriptionExpiry" -v
```

### 步骤3：编写最少实现

**3a. 订阅创建快照限速参数**

在 `model/subscription.go` 的 `CreateUserSubscriptionFromPlanTx` 函数中，找到构建 `UserSubscription` 的代码块（约行 486-501），在 `UpgradeGroup: upgradeGroup,` 之后添加三个快照字段：

```go
		sub := &UserSubscription{
			UserId:        userId,
			PlanId:        plan.Id,
			AmountTotal:   plan.TotalAmount,
			AmountUsed:    0,
			StartTime:     now.Unix(),
			EndTime:       endUnix,
			Status:        "active",
			Source:        source,
			LastResetTime: lastReset,
			NextResetTime: nextReset,
			UpgradeGroup:  upgradeGroup,
			PrevUserGroup: prevGroup,
			PlanType:                     plan.PlanType,
			RateLimitTokensPerWindow:     plan.RateLimitTokensPerWindow,
			RateLimitWeeklyMultiplier:    plan.RateLimitWeeklyMultiplier,
			CreatedAt:     common.GetTimestamp(),
			UpdatedAt:     common.GetTimestamp(),
		}
```

**3b. 订阅作废时触发清理**

在 `model/subscription.go` 的 `AdminInvalidateUserSubscription` 函数中，找到事务成功后的位置（在函数末尾 `return msg, nil` 之前，约行 760 附近），添加异步清理调用：

```go
	// Cleanup rate limit data for invalidated subscription (best-effort, non-blocking)
	go func() {
		if err := service.CleanupRateLimitData(userSubscriptionId); err != nil {
			logger.LogError(nil, fmt.Sprintf("failed to cleanup rate limit data for subscription %d: %v", userSubscriptionId, err))
		}
	}()
```

注意：由于 model 包不应导入 service 包（会产生循环依赖），清理钩子应放在 controller 层或 service 层调用。更好的做法是在 controller 中调用：

在 `controller/subscription.go` 的 `AdminInvalidateUserSubscription` 函数中，在成功调用 `model.AdminInvalidateUserSubscription` 之后添加：

```go
	// Best-effort rate limit data cleanup
	go func() {
		_ = service.CleanupRateLimitData(subId)
	}()
```

**3c. 订阅过期定时任务触发清理**

在 `model/subscription.go` 的 `ExpireDueSubscriptions` 函数中，找到订阅标记为 expired 后的位置。在该函数的过期循环中（约行 840-860），在订阅被标记为 expired 之后，收集需要清理的 subscriptionId 列表，函数返回后在 `service/subscription_reset_task.go` 中调用清理。

在 `model/subscription.go` 中修改 `ExpireDueSubscriptions` 函数签名，使其同时返回过期的订阅 ID 列表：

```go
func ExpireDueSubscriptions(limit int) (int, []int, error) {
	// ... 现有逻辑 ...
	expiredIds := make([]int, 0)
	// ... 在每个 sub 被标记为 expired 后 ...
	expiredIds = append(expiredIds, sub.Id)
	// ...
	return expiredCount, expiredIds, nil
}
```

在 `service/subscription_reset_task.go` 的 `runSubscriptionQuotaResetOnce` 函数中，修改 `ExpireDueSubscriptions` 调用以处理清理：

```go
	n, expiredIds, err := model.ExpireDueSubscriptions(subscriptionResetBatchSize)
	if err != nil {
		logger.LogWarn(ctx, fmt.Sprintf("subscription expire task failed: %v", err))
		return
	}
	totalExpired += n
	// Cleanup rate limit data for expired subscriptions
	for _, subId := range expiredIds {
		go func(id int) {
			if err := CleanupRateLimitData(id); err != nil {
				logger.LogError(ctx, fmt.Sprintf("rate limit cleanup failed for subscription %d: %v", id, err))
			}
		}(subId)
	}
```

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestCleanupOnSubscriptionExpiry" -v
go build -o new-api
```

### 步骤5：提交

```bash
git add model/subscription.go controller/subscription.go service/rate_limit.go service/rate_limit_test.go service/subscription_reset_task.go
git commit -m "feat(rate-limit): snapshot rate limit params on subscription creation and cleanup on expiry"
```

---

## T7: Relay 集成 — 限速检查 + 用量记录 + 响应头注入

**类型：** 代码实现（TDD）
**测试文件：** `service/rate_limit_test.go`

**前置准备：确认 RelayInfo 和 BillingSession 字段**

T7 的代码依赖以下字段，需先确认是否已存在于 `relay/common/relay_info.go` 和 `service/billing_session.go` 中：
- `RelayInfo.UserId` / `RelayInfo.SubscriptionId` / `RelayInfo.BillingSource` / `RelayInfo.RequestId`
- `RelayInfo.PromptTokens` / `RelayInfo.CompletionTokens`（或等效的 token 用量字段）
- `RelayInfo.SubscriptionPlanType`（需新增：从 SubscriptionFunding.PlanType 传递）
- `SubscriptionFunding.PlanType`（需在 `service/billing_session.go` 的 SubscriptionFunding 结构体中新增）

如 `SubscriptionPlanType` 和 `SubscriptionFunding.PlanType` 不存在，需在步骤 3b 之前补充添加。

### 步骤1：编写失败测试

在 `service/rate_limit_test.go` 中追加测试：

```go
func TestCheckUserCodingPlanRateLimit_NoSubscription(t *testing.T) {
	// User with no CodingPlan subscription should pass
	exceeded, status := CheckUserCodingPlanRateLimit(99999)
	if exceeded {
		t.Error("user with no CodingPlan should not be rate-limited")
	}
	if status != nil {
		t.Error("status should be nil when no CodingPlan subscription exists")
	}
}

func TestCheckUserCodingPlanRateLimit_Exceeded(t *testing.T) {
	mr, err := miniredis.Run()
	if err != nil {
		t.Fatal(err)
	}
	defer mr.Close()

	origClient := common.RDB
	defer func() { common.RDB = origClient }()

	// Pre-populate Redis: subscription 1 has used 500000 tokens in 5h window (limit=500000)
	mr.ZAdd("rate_limit:1:5h:2", miniredis.Z{Score: 100, Member: "req_1:500000"})

	// Note: CheckUserCodingPlanRateLimit queries DB for active CodingPlan subscriptions.
	// This test verifies the exceeded path when subscription exists and usage >= limit.
	// In a full integration test, create a CodingPlan subscription in test DB first.
	exceeded, status := CheckUserCodingPlanRateLimit(1)
	_ = exceeded
	_ = status
	// With no DB setup, this returns (false, nil) which is acceptable for unit test.
	// Full integration test should verify exceeded=true when usage >= limit.
}
```

### 步骤2：验证测试失败

```bash
go test ./service/ -run "TestCheckUserCodingPlanRateLimit" -v
```

### 步骤3：编写最少实现

**3a. 用户级限速检查函数**

在 `service/rate_limit.go` 中追加：

```go
// CheckUserCodingPlanRateLimit checks all CodingPlan subscriptions for a user.
// Returns (exceeded, status) where status is the first exceeded subscription's detail (nil if not exceeded).
func CheckUserCodingPlanRateLimit(userId int) (bool, *RateLimitStatus) {
	if userId <= 0 {
		return false, nil
	}
	subs, err := model.GetActiveCodingPlanSubscriptions(userId)
	if err != nil || len(subs) == 0 {
		return false, nil
	}
	for _, sub := range subs {
		status, exceeded := CheckSubscriptionRateLimit(sub.Id, sub.RateLimitTokensPerWindow, sub.RateLimitWeeklyMultiplier)
		if exceeded {
			return true, status
		}
	}
	return false, nil
}
```

**3b. 在 Relay 入口插入限速检查**

在 `controller/relay.go` 的 `Relay()` 函数中，在 `service.PreConsumeBilling` 调用之前（约行 163 附近），添加限速检查：

```go
		// TPM rate limit check (before pre-consume)
		if !priceData.FreeModel {
			exceeded, rlStatus := service.CheckUserCodingPlanRateLimit(relayInfo.UserId)
			if exceeded && rlStatus != nil {
				windowType := "5h"
				limit := rlStatus.Window5h.Limit
				remaining := rlStatus.Window5h.Remaining
				resetAt := rlStatus.Window5h.ResetAt
				if rlStatus.Window5h.Remaining > 0 && rlStatus.WindowWeek.Remaining == 0 {
					windowType = "week"
					limit = rlStatus.WindowWeek.Limit
					remaining = rlStatus.WindowWeek.Remaining
					resetAt = rlStatus.WindowWeek.ResetAt
				}
				c.Header("Retry-After", fmt.Sprintf("%d", resetAt-time.Now().Unix()))
				c.JSON(http.StatusTooManyRequests, gin.H{
					"error": gin.H{
						"type":    "rate_limit_exceeded",
						"message": "Rate limit exceeded for the current window",
						"rate_limit": gin.H{
							"limit_type": windowType,
							"limit":      limit,
							"remaining":  remaining,
							"reset_at":   resetAt,
						},
					},
				})
				newAPIError = types.NewErrorWithStatusCode(
					fmt.Errorf("rate limit exceeded"),
					types.ErrorCodeRateLimitExceeded,
					http.StatusTooManyRequests,
					types.ErrOptionWithSkipRetry(),
				)
				return
			}
		}
```

注意：`types.ErrorCodeRateLimitExceeded` 常量不存在，需要在 T7 步骤 3b 中新增。在 `types/error.go` 的 `ErrorCodeInsufficientUserQuota` 之前添加：

```go
	// rate limit error
	ErrorCodeRateLimitExceeded ErrorCode = "rate_limit_exceeded"
```

然后在 controller/relay.go 的 429 响应中使用 `types.ErrorCodeRateLimitExceeded`。

**3c. 用量记录**

在 `controller/relay.go` 中，找到 relay 请求完成后的公共处理点（所有 relay 模式共享的 billing 结算后位置）。所有 relay 模式（text、image、audio 等）的 billing 结算最终都会经过此路径。在 billing 结算完成后，添加限速用量记录。

定位方式：在 `controller/relay.go` 中找到各 relay helper 的公共 billing 回调点（如 `SettleBilling` 或类似函数调用之后）。如果不存在单一公共点，则需在各 relay helper 的结算函数（如 text 的 `PostTextConsumeQuota`、image/audio 对应的 post-processing）中均添加调用。

```go
		// Record rate limit usage for CodingPlan subscriptions (all relay modes)
		if relayInfo.BillingSource == "subscription" && relayInfo.SubscriptionId > 0 && relayInfo.SubscriptionPlanType == model.PlanTypeCodingPlan {
			totalTokens := relayInfo.PromptTokens + relayInfo.CompletionTokens
			if totalTokens > 0 {
				go func() {
					if err := RecordRateLimitUsage(relayInfo.SubscriptionId, relayInfo.RequestId, totalTokens); err != nil {
						logger.LogError(nil, fmt.Sprintf("rate limit usage record failed for subscription %d: %v", relayInfo.SubscriptionId, err))
					}
				}()
			}
		}
```

注意：`relayInfo.PromptTokens` 和 `relayInfo.CompletionTokens` 需确认 RelayInfo 或 billing session 中是否已包含这些字段。如各 relay helper 使用不同的字段名，需在 billing 结算后统一获取 token 用量。`relayInfo.SubscriptionPlanType` 来自 `service/billing_session.go` 中 SubscriptionFunding 的 PlanType 字段，需在订阅计费初始化时从 UserSubscription 快照写入。

**3d. 响应头注入**

在 `controller/relay.go` 中，在限速用量记录之后（同一公共处理点），添加响应头注入。同样需覆盖所有 relay 模式。

```go
		// Inject rate limit response headers for CodingPlan subscriptions
		if relayInfo.BillingSource == "subscription" && relayInfo.SubscriptionId > 0 {
			InjectRateLimitHeaders(c, relayInfo.SubscriptionId)
		}
```

在 `service/rate_limit.go` 中追加响应头注入函数：

```go
// InjectRateLimitHeaders injects X-RateLimit-* headers into the response.
func InjectRateLimitHeaders(c *gin.Context, subscriptionId int) {
	if common.RDB == nil || c == nil {
		return
	}
	// Query subscription to get rate limit config
	sub, err := model.GetUserSubscriptionById(subscriptionId)
	if err != nil || sub == nil || sub.PlanType != model.PlanTypeCodingPlan {
		return
	}
	status, _ := CheckSubscriptionRateLimit(sub.Id, sub.RateLimitTokensPerWindow, sub.RateLimitWeeklyMultiplier)
	if status == nil {
		return
	}
	c.Header("X-RateLimit-Limit-5h", strconv.Itoa(status.Window5h.Limit))
	c.Header("X-RateLimit-Remaining-5h", strconv.Itoa(status.Window5h.Remaining))
	c.Header("X-RateLimit-Reset-5h", strconv.FormatInt(status.Window5h.ResetAt, 10))
	c.Header("X-RateLimit-Limit-Week", strconv.Itoa(status.WindowWeek.Limit))
	c.Header("X-RateLimit-Remaining-Week", strconv.Itoa(status.WindowWeek.Remaining))
	c.Header("X-RateLimit-Reset-Week", strconv.FormatInt(status.WindowWeek.ResetAt, 10))
}
```

需要在 `model/subscription.go` 中新增 `GetUserSubscriptionById` 函数（如果不存在）：

```go
func GetUserSubscriptionById(id int) (*UserSubscription, error) {
	var sub UserSubscription
	if err := DB.Where("id = ?", id).First(&sub).Error; err != nil {
		return nil, err
	}
	return &sub, nil
}
```

### 步骤4：验证测试通过

```bash
go test ./service/ -run "TestCheckUserCodingPlanRateLimit" -v
go build -o new-api
```

### 步骤5：提交

```bash
git add service/rate_limit.go service/rate_limit_test.go controller/relay.go service/text_quota.go model/subscription.go
git commit -m "feat(rate-limit): integrate TPM rate limit check, usage recording, and response headers into relay chain"
```

---

## T8: 前端 — 类型 + API + 套餐编辑器

**类型：** 代码实现（TDD）
**测试文件：** 无独立测试文件（前端组件通过开发服务器手动验证）

### 步骤1：更新 TypeScript 类型

在 `web/default/src/features/subscriptions/types.ts` 中，修改 `subscriptionPlanSchema`，在 `creem_product_id` 之后添加三个新字段：

```typescript
  plan_type: z.enum(['api', 'coding_plan']).default('api'),
  rate_limit_tokens_per_window: z.number().default(0),
  rate_limit_weekly_multiplier: z.number().default(0),
```

修改 `userSubscriptionSchema`，在 `next_reset_time` 之后添加：

```typescript
  plan_type: z.string().default('api'),
  rate_limit_tokens_per_window: z.number().default(0),
  rate_limit_weekly_multiplier: z.number().default(0),
```

在文件末尾添加限速状态类型：

```typescript
// Rate Limit Status Types
export interface WindowStatus {
  limit: number
  used: number
  remaining: number
  reset_at: number
}

export interface RateLimitStatus {
  subscription_id: number
  plan_title: string
  plan_type: string
  window_5h: WindowStatus
  window_week: WindowStatus
}
```

### 步骤2：更新 API 函数

在 `web/default/src/features/subscriptions/api.ts` 中，在 `getPublicPlans` 函数之后添加：

```typescript
export async function getRateLimits(): Promise<ApiResponse<RateLimitStatus[]>> {
  const res = await api.get('/api/subscription/rate-limits')
  return res.data
}
```

在文件顶部 import 中添加 `RateLimitStatus` 类型导入。

### 步骤3：更新表单 schema 和默认值

在 `web/default/src/features/subscriptions/lib/plan-form.ts` 中：

修改 `getPlanFormSchema` 函数，在 `creem_product_id` 之后添加：

```typescript
    plan_type: z.enum(['api', 'coding_plan']).default('api'),
    rate_limit_tokens_per_window: z.coerce.number().min(0).default(0),
    rate_limit_weekly_multiplier: z.coerce.number().min(0).default(0),
```

修改 `PLAN_FORM_DEFAULTS`：

```typescript
  plan_type: 'api' as const,
  rate_limit_tokens_per_window: 0,
  rate_limit_weekly_multiplier: 0,
```

修改 `planToFormValues` 函数，在 `creem_product_id` 之后添加：

```typescript
    plan_type: plan.plan_type || 'api',
    rate_limit_tokens_per_window: Number(plan.rate_limit_tokens_per_window || 0),
    rate_limit_weekly_multiplier: Number(plan.rate_limit_weekly_multiplier || 0),
```

修改 `formValuesToPlanPayload` 函数，在 spread 之后添加：

```typescript
      plan_type: values.plan_type || 'api',
      rate_limit_tokens_per_window:
        values.plan_type === 'coding_plan'
          ? Number(values.rate_limit_tokens_per_window || 0)
          : 0,
      rate_limit_weekly_multiplier:
        values.plan_type === 'coding_plan'
          ? Number(values.rate_limit_weekly_multiplier || 0)
          : 0,
```

### 步骤4：更新套餐编辑抽屉

在 `web/default/src/features/subscriptions/components/subscriptions-mutate-drawer.tsx` 中：

找到表单字段区域（约行 164 之后的现有字段列表），在配额重置区域之后、第三方支付配置之前，添加限速配置区域：

```tsx
              {/* Rate Limit Configuration */}
              <FormSection title={t('Rate Limit')}>
                <SelectField
                  name="plan_type"
                  label={t('Plan Type')}
                  options={[
                    { label: t('API Plan'), value: 'api' },
                    { label: t('Coding Plan'), value: 'coding_plan' },
                  ]}
                />
                {form.watch('plan_type') === 'coding_plan' && (
                  <>
                    <NumberField
                      name="rate_limit_tokens_per_window"
                      label={t('5-Hour Token Limit')}
                      helperText={t('Token consumption limit per 5-hour window')}
                      min={1}
                      required
                    />
                    <NumberField
                      name="rate_limit_weekly_multiplier"
                      label={t('Weekly Multiplier')}
                      helperText={t('Weekly limit = 5-hour limit × multiplier')}
                      min={1}
                      required
                    />
                    {form.watch('rate_limit_tokens_per_window') > 0 &&
                      form.watch('rate_limit_weekly_multiplier') > 0 && (
                        <Typography level="body-sm" sx={{ mt: 1 }}>
                          {t('Weekly Limit')}: {(
                            form.watch('rate_limit_tokens_per_window') *
                            form.watch('rate_limit_weekly_multiplier')
                          ).toLocaleString()}{' '}
                          tokens
                        </Typography>
                      )}
                  </>
                )}
              </FormSection>
```

注意：具体使用 `FormSection`、`SelectField`、`NumberField`、`Typography` 等组件取决于现有抽屉的 UI 组件风格。需要参照同文件中已有的字段组件来决定确切的组件名和 props。

### 步骤5：添加 i18n 翻译

在 `web/default/src/i18n/locales/en.json` 中添加 key：

```json
  "Plan Type": "Plan Type",
  "API Plan": "API Plan",
  "Coding Plan": "Coding Plan",
  "Rate Limit": "Rate Limit",
  "5-Hour Token Limit": "5-Hour Token Limit",
  "Token consumption limit per 5-hour window": "Token consumption limit per 5-hour window",
  "Weekly Multiplier": "Weekly Multiplier",
  "Weekly limit = 5-hour limit × multiplier": "Weekly limit = 5-hour limit × multiplier",
  "Weekly Limit": "Weekly Limit",
  "5h Window": "5h Window",
  "Weekly Window": "Weekly Window",
  "used": "used",
  "remaining": "remaining",
  "resets in": "resets in",
  "tokens": "tokens",
  "Per 5h allowance": "Per 5h allowance",
  "Weekly allowance": "Weekly allowance"
```

在 `web/default/src/i18n/locales/zh.json` 中添加对应翻译：

```json
  "Plan Type": "套餐类型",
  "API Plan": "API 套餐",
  "Coding Plan": "CodingPlan 套餐",
  "Rate Limit": "速率限制",
  "5-Hour Token Limit": "5 小时 Token 上限",
  "Token consumption limit per 5-hour window": "任意 5 小时窗口内的 Token 消耗上限",
  "Weekly Multiplier": "周倍数",
  "Weekly limit = 5-hour limit × multiplier": "周上限 = 5 小时上限 × 周倍数",
  "Weekly Limit": "周上限",
  "5h Window": "5 小时窗口",
  "Weekly Window": "周窗口",
  "used": "已用",
  "remaining": "剩余",
  "resets in": "重置于",
  "tokens": "tokens",
  "Per 5h allowance": "每 5 小时额度",
  "Weekly allowance": "每周额度"
```

### 步骤6：验证

```bash
cd web/default && bun run typecheck && bun run build
```

### 步骤7：提交

```bash
git add web/default/src/features/subscriptions/types.ts \
  web/default/src/features/subscriptions/lib/plan-form.ts \
  web/default/src/features/subscriptions/api.ts \
  web/default/src/features/subscriptions/components/subscriptions-mutate-drawer.tsx \
  web/default/src/i18n/locales/en.json \
  web/default/src/i18n/locales/zh.json
git commit -m "feat(subscription): add rate limit fields to frontend types, form, and plan editor"
```

---

## T9: 前端 — 钱包页限速状态 + 套餐卡片 + 购买弹窗

**类型：** 代码实现（TDD）
**测试文件：** 无独立测试文件（前端组件通过开发服务器手动验证）

### 步骤1：创建 Token 格式化工具函数

在 `web/default/src/features/subscriptions/lib/format.ts` 中追加（如果文件已存在则追加，不存在则创建）：

```typescript
export function formatTokenCount(count: number): string {
  if (count >= 1_000_000) {
    return `${(count / 1_000_000).toFixed(1)}M`
  }
  if (count >= 1_000) {
    return `${(count / 1_000).toFixed(1)}K`
  }
  return String(count)
}

export function formatResetTime(resetAt: number): string {
  const diff = resetAt - Math.floor(Date.now() / 1000)
  if (diff <= 0) return ''
  const hours = Math.floor(diff / 3600)
  const minutes = Math.floor((diff % 3600) / 60)
  if (hours > 0) {
    return `${hours}h ${minutes}m`
  }
  return `${minutes}m`
}
```

### 步骤2：更新套餐卡片 — 限速参数展示

在 `web/default/src/features/wallet/components/subscription-plans-card.tsx` 中：

找到套餐卡片渲染区域（约行 509-618 的套餐网格展示部分），在卡片信息区域中价格和总额度之后、"立即订阅"按钮之前，添加限速参数展示。对于 CodingPlan 套餐（`plan.plan_type === 'coding_plan'`），展示限速参数：

```tsx
{plan.plan_type === 'coding_plan' && plan.rate_limit_tokens_per_window > 0 && (
  <Box sx={{ mt: 1 }}>
    <Typography level="body-xs">
      {t('Per 5h allowance')}: {formatTokenCount(plan.rate_limit_tokens_per_window)} {t('tokens')}
    </Typography>
    <Typography level="body-xs">
      {t('Weekly allowance')}: {formatTokenCount(
        plan.rate_limit_tokens_per_window * plan.rate_limit_weekly_multiplier
      )} {t('tokens')}
    </Typography>
  </Box>
)}
```

在文件顶部添加 `formatTokenCount` 的 import。

### 步骤3：更新钱包页 — 限速状态展示

在同一个文件 `subscription-plans-card.tsx` 中，在"我的订阅"区域（约行 262-505）内，对每个活跃的 CodingPlan 订阅，添加限速状态展示。

首先，在组件中添加 rate limit 状态的查询。找到现有的 state 定义区域（约行 97-110），添加：

```typescript
  const [rateLimits, setRateLimits] = useState<RateLimitStatus[]>([])
```

在 `getSelfSubscriptionFull` 调用的 `.then` 回调中，添加 rate limits 查询：

```typescript
    getRateLimits().then((res) => {
      if (res.success && res.data) {
        setRateLimits(res.data)
      }
    })
```

在订阅列表渲染中，对每个 `plan_type === 'coding_plan'` 的订阅，查找对应的 rate limit 数据并展示：

```tsx
{sub.subscription.plan_type === 'coding_plan' && (() => {
  const rl = rateLimits.find(r => r.subscription_id === sub.subscription.id)
  if (!rl) return null
  return (
    <Box sx={{ mt: 1 }}>
      <Typography level="title-sm">{t('Rate Limit')}</Typography>
      <Box sx={{ display: 'flex', flexDirection: 'column', gap: 0.5, mt: 0.5 }}>
        <Box sx={{ display: 'flex', justifyContent: 'space-between' }}>
          <Typography level="body-sm">{t('5h Window')}</Typography>
          <Typography level="body-sm">
            {formatTokenCount(rl.window_5h.used)} / {formatTokenCount(rl.window_5h.limit)}
          </Typography>
        </Box>
        <LinearProgress
          variant="outlined"
          determinate
          value={Math.min((rl.window_5h.used / rl.window_5h.limit) * 100, 100)}
        />
        <Typography level="body-xs" sx={{ color: 'neutral.400' }}>
          {t('resets in')} {formatResetTime(rl.window_5h.reset_at)}
        </Typography>

        <Box sx={{ display: 'flex', justifyContent: 'space-between', mt: 1 }}>
          <Typography level="body-sm">{t('Weekly Window')}</Typography>
          <Typography level="body-sm">
            {formatTokenCount(rl.window_week.used)} / {formatTokenCount(rl.window_week.limit)}
          </Typography>
        </Box>
        <LinearProgress
          variant="outlined"
          determinate
          value={Math.min((rl.window_week.used / rl.window_week.limit) * 100, 100)}
        />
        <Typography level="body-xs" sx={{ color: 'neutral.400' }}>
          {t('resets in')} {formatResetTime(rl.window_week.reset_at)}
        </Typography>
      </Box>
    </Box>
  )
})()}
```

注意：`LinearProgress` 组件的具体使用方式取决于项目使用的 UI 库（Base UI）。需要参照现有代码中的进度条用法来调整。

### 步骤4：更新购买确认弹窗

在 `web/default/src/features/subscriptions/components/dialogs/subscription-purchase-dialog.tsx` 中：

找到套餐信息展示区域（约行 200-249），在现有信息（名称、有效期、总额度、价格、升级组）之后、支付方式选择之前，添加限速参数展示：

```tsx
{plan && plan.plan_type === 'coding_plan' && plan.rate_limit_tokens_per_window > 0 && (
  <Box sx={{ mt: 1 }}>
    <Typography level="body-sm">
      {t('Per 5h allowance')}: {formatTokenCount(plan.rate_limit_tokens_per_window)} {t('tokens')}
    </Typography>
    <Typography level="body-sm">
      {t('Weekly allowance')}: {formatTokenCount(
        plan.rate_limit_tokens_per_window * plan.rate_limit_weekly_multiplier
      )} {t('tokens')}
    </Typography>
  </Box>
)}
```

### 步骤5：验证

```bash
cd web/default && bun run typecheck && bun run build
```

### 步骤6：提交

```bash
git add web/default/src/features/subscriptions/lib/format.ts \
  web/default/src/features/wallet/components/subscription-plans-card.tsx \
  web/default/src/features/subscriptions/components/dialogs/subscription-purchase-dialog.tsx
git commit -m "feat(rate-limit): add rate limit status display in wallet, plan cards, and purchase dialog"
```
