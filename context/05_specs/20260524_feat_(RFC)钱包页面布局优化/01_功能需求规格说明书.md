# 钱包页面布局优化 功能需求规格说明书

## 文档信息

- 基线 Feature：20260522_feat_钱包页面
- 变更组：无

---

## 变更说明

> 本文档基于基线 `20260522_feat_钱包页面` 生成，属于变更版本。
>
> **变更概述**：调整钱包页面布局，将兑换码充值从充值表单卡片中拆出为独立全宽区域并置于上方，在线充值与订阅计划并排展示。

### 变更摘要

| 变更类型 | 涉及章节 | 变更内容 |
|---------|---------|---------|
| **[无变化]** | 1. 概述 | 无变更 |
| **[无变化]** | 2. 角色与权限矩阵 | 无变更 |
| **[修改]** | 3. 页面与功能总览 | 3.2 页面跳转流程中，兑换码充值从充值表单卡片内拆出为独立区域 |
| **[修改]** | 4. 页面功能详细说明 | 4.1 钱包页面：4.1.1 页面职责布局描述更新；4.1.2 字段分区重组（兑换码独立为新区段）；4.1.3 兑换码充值功能位置更新；4.1.5 交互逻辑中布局描述更新 |
| **[无变化]** | 4.2-4.5 其他页面 | 无变更 |
| **[无变化]** | 5. 非页面功能详细说明 | 无变更 |
| **[无变化]** | 6. 数据状态定义 | 无变更 |
| **[无变化]** | 7. 集成和依赖 | 无变更 |
| **[修改]** | 8. 附录 | 变更记录追加新版本 |

---

## 1. 概述

> **[无变化]** 本章内容与基线版本一致，包含功能背景和业务目标。详见基线文档。

---

## 2. 角色与权限矩阵

> **[无变化]** 本章内容与基线版本一致，包含2.1数据权限（行级访问控制）和2.2页面访问权限。详见基线文档。

---

## 3. 页面与功能总览

### 3.1 页面清单

> **[无变化]** 页面清单和页面形式无变更。详见基线文档。

### 3.2 页面跳转流程

> [修改] 兑换码充值从充值表单卡片内拆出为独立区域

```
[入口：侧边栏/导航 - Wallet] → [钱包页面]
                                      ├─ 余额概览卡片 → 自动加载
                                      ├─ 兑换码充值区域（独立全宽卡片）
                                      │   └─ 兑换码输入 + 点击"Redeem" → 直接完成（不打开弹窗）
                                      ├─ 在线充值卡片 + 订阅计划卡片（并排布局）
                                      │   ├─ 点击"支付方式"按钮 → [支付确认弹窗] → 确认 → 跳转第三方支付页面
                                      │   ├─ 点击"Creem产品" → [Creem产品确认弹窗] → 确认 → 跳转 Creem 结账页面
                                      │   ├─ 点击"Order History"按钮 → [账单历史弹窗]
                                      │   └─ 点击"Subscribe Now" → [订阅购买弹窗（由订阅功能管理）]
                                      └─ 推荐奖励卡片 → 点击"Transfer to Balance" → [额度转赠弹窗] → 确认 → 完成转赠
```

**流程说明：**

> [修改] 第3、5点中兑换码相关流程描述更新

1. 用户通过导航菜单进入钱包页面
2. 页面加载时自动获取用户余额数据和充值配置
3. 用户可在兑换码充值区域输入兑换码完成充值（独立于在线充值）
4. 用户选择充值金额和支付方式后，系统弹出支付确认弹窗
5. 支付完成后，第三方通过 Webhook 异步回调系统，系统更新订单状态并增加用户余额
6. 用户从第三方支付页面返回时，系统通过 `show_history` URL 参数自动打开账单历史弹窗（如 `/wallet?show_history=true`），并刷新用户余额数据
7. 用户可随时打开账单历史弹窗查看交易记录

---

## 4. 页面功能详细说明

### 4.1 页面 1：钱包页面

#### 4.1.1 页面概述

> [修改] 页面职责描述中布局结构更新

**页面形式：**独立页面

**页面职责：**用户资金管理的核心页面，上方展示账户余额概览（当前余额、总使用量、API请求总数），中部第一行为兑换码充值独立区域（全宽），第二行左侧为在线充值表单（API套餐）、右侧为订阅计划卡片（订阅套餐），下方展示推荐奖励卡片（推荐链接、奖励统计、奖励转赠）。

#### 4.1.2 涉及字段

> [修改] 充值表单区域字段拆分为"在线充值区域字段"和"兑换码区域字段"两个独立区段；其余区段无变化

##### A. 钱包概览区域字段

> **[无变化]** 与基线版本一致。详见基线文档。

##### B. 在线充值区域字段

> [修改] 原"充值表单区域字段"重命名为"在线充值区域字段"，移除兑换码字段至独立区段

| 字段名称 | 字段类型 | 数据来源 | 获取时机 | 校验规则 | 默认值 | 业务含义 |
|---------|---------|---------|---------|---------|--------|--------|
| 预设金额选项 | 选择按钮组 | 系统预设（由后端 amount_options 配置） | 页面加载 | 无 | 第一个预设金额 | 快捷选择充值金额，点击即选中并计算实付金额 |
| 自定义金额 | 数字输入框 | 用户输入 | 实时输入 | >= 当前支付渠道最小充值金额；整数 | 当前支付渠道最小充值金额 | 用户自定义充值金额 |
| 支付方式 | 按钮组 | 系统预设（由后端 pay_methods 配置） | 页面加载 | 无 | 第一个可用支付方式 | 选择充值所使用的支付渠道 |
| Waffo支付方式 | 按钮组 | 系统预设（由后端 waffo_pay_methods 配置） | 页面加载 | 无 | 无 | Waffo 渠道的子支付方式选择 |
| Creem产品 | 卡片列表 | 系统预设（由后端 creem_products 配置） | 页面加载 | 无 | 无 | Creem 加密货币渠道的固定面额产品列表 |
| 实付金额 | 标签（计算字段） | 支付渠道金额计算接口返回（POST /api/user/amount、/stripe/amount、/waffo-pancake/amount） | 充值金额或支付方式变化时 | 无 | 无 | 前端调用后端金额计算接口获取实付金额，非本地计算 |

**充值配置开关字段（由后端 TopupInfo 返回，控制页面区域可见性）：**

| 配置字段 | 类型 | 业务含义 |
|---------|------|---------|
| enable_online_topup | 布尔 | 是否启用易支付在线充值 |
| enable_stripe_topup | 布尔 | 是否启用 Stripe 信用卡充值 |
| enable_creem_topup | 布尔 | 是否启用 Creem 加密货币充值 |
| enable_waffo_topup | 布尔 | 是否启用 Waffo 充值 |
| enable_waffo_pancake_topup | 布尔 | 是否启用 Waffo Pancake 充值 |
| enable_redemption | 布尔 | 是否启用兑换码功能 |
| payment_compliance_confirmed | 布尔 | 是否已完成支付合规确认 |

##### C. 兑换码区域字段

> **[新增]** 兑换码字段从原"充值表单区域字段"拆出，作为独立区域

| 字段名称 | 字段类型 | 数据来源 | 获取时机 | 校验规则 | 默认值 | 业务含义 |
|---------|---------|---------|---------|---------|--------|--------|
| 兑换码 | 文本输入框 | 用户输入 | 实时输入 | 非空 | 空 | 输入兑换码进行充值 |

##### D. 订阅计划区域字段

> **[无变化]** 与基线版本"订阅计划区域字段"一致。详见基线文档。

##### E. 推荐奖励区域字段

> **[无变化]** 与基线版本"推荐奖励区域字段"一致。详见基线文档。

#### 4.1.3 功能与按钮

> [修改] 兑换码充值功能的卡片上下文更新；其余功能无变化

**用户可操作功能：**

- 选择预设金额、输入自定义金额、选择支付方式并发起支付、选择 Waffo 支付方式并发起支付、选择 Waffo Pancake 充值、选择 Creem 产品并发起支付、打开账单历史、切换计费偏好、刷新订阅状态、购买订阅计划、复制推荐链接、转赠奖励到余额
  - 以上功能均与基线版本一致，详见基线文档。

- **[修改] 兑换码充值**
  - 触发按钮："Redeem" 按钮
  - 按钮级别：字段级
  - 按钮位置：兑换码输入框右侧（位于独立全宽兑换码卡片内）
  - 触发方式：点击按钮
  - 权限要求：见 2.2 页面访问权限
  - 加载状态：点击后按钮显示 loading 旋转图标
  - 功能说明：输入兑换码后点击"Redeem"，系统验证兑换码有效性。成功后自动清空输入框、刷新余额数据，并提示成功充值的配额数量（i18n key: "Redemption successful! Added: {{quota}}"）。验证失败时显示 toast 错误提示（i18n key: "Redemption failed"）

**后台自动流程：**

- 页面初始化加载、支付返回后余额刷新
  - 与基线版本一致，详见基线文档。

- **[修改] 兑换码获取链接展示**
  - 触发时机：页面加载完成，topup_link 配置存在时
  - 说明：在独立兑换码充值卡片下方显示"Need a redemption code? Get one here"外部链接

#### 4.1.4 业务规则

> **[无变化]** 本章内容与基线版本一致，包含折扣系统、最小充值金额校验、订单过期时间、支付渠道可用性控制、支付方式选择与金额计算联动、兑换码验证、配额换算、支付合规控制共8条规则。详见基线文档。

#### 4.1.5 交互逻辑

> [修改] 布局相关交互逻辑更新

- 预设金额卡片采用 2 列（移动端）/ 4 列（桌面端）网格布局，选中状态的卡片有边框和背景色变化
- 自定义金额输入框旁实时显示"Amount to pay:"和计算中的骨架屏或实付金额
- Creem 产品区域以独立卡片列表形式展示，与标准支付方式区域通过分隔线区分
- 当在线充值全部关闭时，整个在线充值区域替换为 Alert 提示
- 页面加载期间，概览卡片、兑换码卡片、充值表单、订阅计划卡片和推荐奖励卡片均显示骨架屏
- **[新增]** 兑换码充值区域为独立全宽卡片，位于余额概览下方、在线充值和订阅计划卡片上方。当 enable_redemption 为 false 时，该卡片不渲染
- **[修改]** 在线充值卡片与订阅计划卡片在桌面端（xl）采用双栏布局（在线充值左侧、订阅计划右侧），当无可用订阅计划时订阅卡片不渲染，在线充值占满宽度
- 推荐奖励卡片位于在线充值和订阅计划下方，采用三栏布局（左侧标题+描述、中间统计数据、右侧链接+操作按钮），移动端纵向堆叠
- 推荐奖励统计数据采用三等分网格展示（Pending/Total Earned/Invites）
- 推荐奖励区域的合规提示仅在合规未确认时显示
- 无活跃订阅时，订阅优先/仅订阅选项标注"(No Active)"，并在下方显示提示文案
- **[删除]** ~~兑换码区域通过分隔线与在线充值区域区分~~（兑换码已独立为单独卡片）

#### 4.1.6 权限要求

> **[无变化]** 与基线版本一致，无特殊权限。详见基线文档。

---

### 4.2 页面 2：支付确认弹窗

> **[无变化]** 本章内容与基线版本一致。详见基线文档。

---

### 4.3 页面 3：Creem产品确认弹窗

> **[无变化]** 本章内容与基线版本一致。详见基线文档。

---

### 4.4 页面 4：账单历史弹窗

> **[无变化]** 本章内容与基线版本一致。详见基线文档。

---

### 4.5 页面 5：额度转赠弹窗

> **[无变化]** 本章内容与基线版本一致。详见基线文档。

---

## 5. 非页面功能详细说明

> **[无变化]** 本章内容与基线版本一致，包含支付回调 Webhook 处理。详见基线文档。

---

## 6. 数据状态定义

> **[无变化]** 本章内容与基线版本一致，包含 pending/success/failed/expired 四种状态。详见基线文档。

---

## 7. 集成和依赖

> **[无变化]** 本章内容与基线版本一致，包含外部系统集成（易支付/Stripe/Creem/Waffo）和内部功能依赖。详见基线文档。

---

## 8. 附录

### 8.1 术语表

> **[无变化]** 与基线版本一致。详见基线文档。

### 8.2 参考文档

| 文档名称 | 类型 | 来源 | 用途 | 说明 |
|---------|------|------|------|------|
| 01_通用需求.md | 规范 | context/05_specs/ | 通用交互与UI规范 | 全系统通用规范基准 |
| 20260522_feat_钱包页面 | 基线文档 | context/05_specs/ | 变更基线 | 本文档基于该基线生成 |

### 8.3 通用交互与UI规范

全系统级别的UI和交互约定详见：[01_通用需求.md](../01_通用需求.md)

**偏离记录：**
- 无偏离

### 8.4 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| v1.0 | 2026-05-22 | 初始版本，从现有实现逆推生成 | lixuetao |
| v1.1 | 2026-05-22 | 监理扫描后基于代码修正 | lixuetao |
| v2.0 | 2026-05-25 | 布局优化：兑换码充值从充值表单卡片中拆出为独立全宽区域置于上方，在线充值与订阅套餐并排展示 | lixuetao |
